# Evidence Contract

Each material run receives one bounded bundle containing:

- image digest;
- toolkit and track version;
- card/input hash;
- prompt and schema version;
- numeric seed;
- House request count;
- failure labels and metrics;
- output hash;
- concise acceptance summary.

Never store credentials, model tokens or unredacted secrets.

