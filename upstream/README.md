# Upstream Pins

This directory is reserved for pinned official track repositories and the shared toolkit.

- Do not edit upstream source in place.
- Record URL, commit/release, retrieval date and content hash in `sources/SOURCE_REGISTRY.md`.
- Upgrade pins through a new ADR or explicit compatibility record.
- Local adapters belong in `tracks/`, never here.

