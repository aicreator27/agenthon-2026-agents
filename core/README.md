# Reusable Core Boundary

Planned responsibilities: House client and budgeting, execution/diagnostics, executable finance registry, logging, provenance, deterministic RNG and shared schema primitives.

`core/` must not depend on Agenthon CLI names, mount paths, submission descriptors or track packages.

